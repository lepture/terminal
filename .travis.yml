language: python
install:
    - pip install coveralls

python:
    - "2.6"
    - "2.7"
    - "3.2"

script:
    - nosetests -s

after_success:
    - coverage run --source=coveralls setup.py -q nosetests
    - coveralls

notifications:
    email: false
